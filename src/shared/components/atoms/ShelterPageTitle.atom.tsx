import { Typography } from "@mui/material";
import React from "react";

const ShelterPageTitle: React.FC = ({children}) => <Typography align="center" gutterBottom variant="h2">{children}</Typography>;

export default ShelterPageTitle;